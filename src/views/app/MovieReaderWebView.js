import React from 'react'
import { View, StyleSheet } from 'react-native'

// unfinished

function MovieReaderWebView (props) {
  return (
    <View style={ s.container }>
      
    </View>
  )
}

const s = StyleSheet.create({
  container: {
    
  }
})

export default MovieReaderWebView
